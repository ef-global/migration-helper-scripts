---
"migration-helper-scripts": patch
---

Add `version` command support and improve CLI command routing compatibility so `diff`, `help`, and `version` also work when invoked through legacy audit-cli entrypoints.
